import chess

#intentando pushear un cambio para acceder a mi gh desde linux
class Engine:
    def __init__(self, board, maxDepth, color):
        self.board = board
        self.maxdDepth = maxDepth
        self. color = color
